// Pengaturan Global Game Pahlawan Bystander

export const GAME_CONFIG = {
  // Waktu hitung mundur (countdown) dalam detik untuk setiap stage
  COUNTDOWN_TIMER_SECONDS: 45,
  
  // Kecepatan efek mengetik (typing effect) dalam milidetik (ms) per huruf
  // Semakin kecil angkanya, semakin cepat teks muncul (rekomendasi: 30 - 80)
  TYPING_SPEED_MS: 60
};
